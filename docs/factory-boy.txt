Available faker providers:
    https://faker.readthedocs.io/en/master/providers/faker.providers.python.html
    name, sentence, pyint, company,
    catch_phrase, image_url, password,
    latitude, longitude,

How to set fake data manually for field?
    Just write it into the class. See the example bellow:

        class UserFactory(factory.django.DjangoModelFactory):
            first_name = factory.Faker("name")
        ->  phone_number = "+989032567181"


How to create multiple objects in one line?
    UserFactory.create_batch(10)